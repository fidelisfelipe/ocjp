package ocjp.collectionsgeneric;

import java.util.Arrays;

public class ArraysMethods{
	public ArraysMethods() {
		Object arrayForConvert = null;
		Object[] a1 = null;
		Object[] a2 = null;
		long[] bytes = null;
		long keySearch = 0;
		
		Arrays.asList(arrayForConvert);
		Arrays.deepEquals(a1, a2);
		Arrays.sort(bytes);
		Arrays.binarySearch(bytes, keySearch);
		
	}
}
